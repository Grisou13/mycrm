module.exports = async (args, opts, logger) => {
   //generate from spec

   // create consul local
   
   // register node in consul local
   // create consul agent for haproxy
   // create haproxy
   
   // register haproxy in main consul cluster
   // create nodes dockerfiles
   // register nodes in local consul

   // create kubernetes deploy file
   // publish to kubernetes api new nodes   
}